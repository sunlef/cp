#include <bits/stdc++.h>
using namespace std;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    string s;
    cin >> s;
    if (s == "Hello,World!") {
        cout << "AC" << '\n';
    } else {
        cout << "WA" << '\n';
    }

    return 0;
}