#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 3000;
    cout << t << '\n';

    for(int i = 1; i <= t; ++i) {
        long long n = 1000000000000000000;
        int k = 16;
        cout << n << ' ' << k << '\n';
    }

    return 0;
}
